import 'package:flutter/material.dart';

class SlapshScreen extends StatelessWidget {
  const SlapshScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('esChat'),
      ),
      body: const Center(
        child: Text('Loading...'),
      ),
    );
  }
}